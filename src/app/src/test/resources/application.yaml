vk:
  confirmation-code: 123456
  secret: test_secret
  api:
    token: vk-token
  bot:
    id: ${VK_BOT_ID:123456789}
    mention: ${VK_BOT_MENTION:@simarel}

summary:
  enabled-chats: 1,2,3

quarkus:
  devservices:
    enabled: true
  datasource:
    db-kind: postgresql
    devservices:
      enabled: true
  observability:
    enabled: false
