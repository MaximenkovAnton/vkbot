package com.simarel.vkbot

import io.quarkus.test.junit.QuarkusTest
import org.junit.jupiter.api.Test

/**
 * Integration test that verifies the application starts correctly.
 */
@QuarkusTest
class InitTest {
    @Test
    fun init() {
        // Test initialization of the app - if we get here, the app started successfully
    }
}
